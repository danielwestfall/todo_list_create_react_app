import { useState } from 'react';

const Listmaker = ({ info, index, remove }) => {

    const [list, setList] = ListArray([]);

    return (
        <>
            {info.thing1}
        </>
    )
}

export default Listmaker
